import { FC } from "react";

type Props = {
    onSelect: (type: string) => void;
}

const SelectRole: FC<Props> =({onSelect}) =>{
    return(
        <div>
            
        </div>
    )
}

export default SelectRole;